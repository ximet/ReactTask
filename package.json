{
  "name": "main",
  "version": "1.0.0",
  "description": "Simple application",
  "main": "ex.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "front": "cd Client && npm start",
    "start": "cd server && nodemon server/index.js",
    "dev": "concurrently --kill-others-on-fail \"npm run start\" \"npm run front\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "concurrently": "^6.4.0"
  },
  "devDependencies": {
    "husky": "^7.0.4"
  }
}
