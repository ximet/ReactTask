const INITIAL_STATE = {
  title: 'World Weather Searcher'
};

export function worldWeatherReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    case 'ActionNAME': {
    }
    default:
      return state;
  }
}
