import { getState } from 'core-js/modules/web.url-search-params';
import { SET_COUNTRY_LIST } from './index';

export const onSearchClick = () => (dispatch, getState) => {
  // dispatch()/ searching start
  // AXIOS /FETCH DATA
  // dispatch(SET_COUNTRY_LIST)/ seT COUNTRIES
  // dispatch()/ searching end
}