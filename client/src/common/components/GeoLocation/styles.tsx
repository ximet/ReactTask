import styled from 'styled-components';

export const GeoLocation = styled.div`
  font-weight: 500;

  svg {
    height: 1rem;
    width: 1rem;
    margin-left: -0.25rem;
    margin-right: 0.5rem;
  }

  p span {
    font-weight: 700;
  }
`;
